<!-- File: Views/AdminDashboard/Users.cshtml (NEW FILE) -->
@model List<BOBDrive.Models.User>
@{
    ViewBag.Title = "User Management";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2><i class="glyphicon glyphicon-user"></i> User Management</h2>
<hr />

<p>
    <a href="@Url.Action("Index", "AdminDashboard")" class="btn btn-default">
        <i class="glyphicon glyphicon-arrow-left"></i> Back to Dashboard
    </a>
</p>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
        <i class="glyphicon glyphicon-ok-sign"></i> @TempData["SuccessMessage"]
    </div>
}

<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">
            All Users
            <span class="badge">@Model.Count</span>
            <span class="badge">Admins: @Model.Count(u => u.Role == "Admin")</span>
        </h3>
    </div>
    <div class="panel-body">
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>User ID</th>
                        <th>Full Name</th>
                        <th>Role</th>
                        <th>External Role ID</th>
                        <th>Created</th>
                        <th>Last Login</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model)
                    {
                        <tr>
                            <td>
                                @user.ExternalUserId
                                @if (user.ExternalUserId == User.Identity.Name)
                                {
                                    <span class="label label-info">You</span>
                                }
                            </td>
                            <td>@user.FullName</td>
                            <td>
                                @if (user.Role == "Admin")
                                {
                                    <span class="label label-danger">Admin</span>
                                }
                                else
                                {
                                    <span class="label label-default">User</span>
                                }
                            </td>
                            <td>@(user.ExternalRoleId.HasValue ? user.ExternalRoleId.Value.ToString() : "N/A")</td>
                            <td>@user.CreatedAt.ToString("yyyy-MM-dd")</td>
                            <td>
                                @if (user.LastLoginAt.HasValue)
                                {
                                    @user.LastLoginAt.Value.ToString("yyyy-MM-dd HH:mm")
                                }
                                else
                                {
                                    <span class="text-muted">Never</span>
                                }
                            </td>
                            <td>
                                <a href="@Url.Action("EditUser", "AdminDashboard", new { id = user.Id })"
                                   class="btn btn-sm btn-primary">
                                    <i class="glyphicon glyphicon-edit"></i> Edit
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>